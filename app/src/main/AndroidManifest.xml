<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nwodhcout.napper.app" >

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NapFeedbackActivity"
            android:label="@string/title_activity_nap_feedback"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.nwodhcout.napper.app.MainActivity" />
        </activity>
        <activity
            android:name=".AlarmReceiverActivity"
            android:label="@string/title_activity_alarm_receiver" >
        </activity>
        <receiver android:name="com.nwodhcout.napper.app.AlarmManagerBroadcastReceiver">
        </receiver>
        <service android:name="com.nwodhcout.napper.app.SimpleWakefulService" >
        </service>
    </application>

</manifest>
